embedding_size: 400
encoder_dims: [200, ]
decoder_dims: [200, ]
anneal_cap: 0.2
total_anneal_steps: 2000000
has_user_emb: False
eval_reparameter_sampling_times: 5  # the argument controls whether to reparameter in evaluation. when <= 0, sampling is abandoned. 